<div class="ui grid">

    <div class="three wide column">

        <div class="ui violet vertical pointing menu">

            <a class="item active" {{action "home"}}>
                <i class="large home icon"></i>
                Home
            </a>
            <a class="item" {{action "studentsDataEntry"}}>
                <i class="large leanpub icon"></i>
                Students Records
            </a>
            <a class="item " {{action "addStudent"}}>
                <i class="large add user icon"></i>
                Add Student
            </a>
            <a class="item " {{action "addResidency"}}>
                <i class="large add user icon"></i>
                Add Residency
            </a>
            <a class="item " {{action "addAward"}}>
            <i class="large add user icon"></i>
            Add Award
            </a>
            <a class="item " {{action "about"}}>
                <i class="large user icon"></i>
                About
            </a>


        </div>

    </div>

    <div class="thirteen wide column ">
        <div class="ui segment container">
            {{#if isHomeShowing}}
                {{welcome-page}}
            {{/if}}
            {{#if isStudentsRecordsDataEntry}}
                {{student-data-entry}}
            {{/if}}
            {{#if isAboutShowing}}
                {{about-us}}
            {{/if}}
            {{#if isAddStudentShowing}}
                {{add-student}}
            {{/if}}
            {{#if isAddResidencyShowing}}
              {{add-residency}}
            {{/if}}
          {{#if isAddAwardShowing}}
            {{add-award}}
          {{/if}}
        </div>
    </div>
</div>
